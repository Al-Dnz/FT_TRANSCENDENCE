version: '3'

services:
  postgres:
      container_name: postgres
      image: postgres:14-alpine3.15
      networks:
        - backend
      # volumes:
      #   - postgres-data
      env_file:
        - ./db/postgres.env
      ports:
        - 5432:5432
      logging:
        driver: none
  auth-api:
    container_name: auth-api
    build:
      context: auth-api
      dockerfile: Dockerfile
    networks:
      - backend
    ports:
      - 3000:3000
    # env_file: <your_env_file>  uncomment this in order to use a .env file
  
  # nestjs:
  #   container_name: nestjs
  #   build:
  #     context: app
  #     dockerfile: Dockerfile
  #   networks:
  #     - backend
  #   ports:
  #     - 3000:3000
  #   depends_on:
  #     - "postgres"
  #   logging:
  #     driver: none

# volumes:
#   postgres-data:

networks:
  backend:
    name: backend_net
    driver: bridge